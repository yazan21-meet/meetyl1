class ball(Turtle):
	def __init__(self,radius,color,dx,dy):
		turtle__init__(self)
		self.radius=radius
		self.color=color
		self.dx=dx
		self.dy=dy
	def move(self,steps):
		self.dx=steps*(self.dx+1)
	def move(self,steps):
		self.dy=steps*(self.dy+1)
		turtle1=turtle(5, blue, 25, 30)
turtle1.penup()
turtle1.shape(circle)



	

